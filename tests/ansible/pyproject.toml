[project]
name = "actions-ansible-smoke-test"
version = "0.0.0"
description = "uv project providing ansible-galaxy for the release smoke-test fixture."
requires-python = ">=3.12"
dependencies = [
    "ansible-core>=2.19",
]
